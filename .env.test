# Committed on purpose. Tests need a database and a cookie secret, and neither
# is a secret when the database is deleted at the start of every run.
COOKIE_SECRET=tests-only-not-a-secret
DB_DRIVER=libsql
DATABASE_URL=file:./data/test.db
SITE_URL=http://localhost
